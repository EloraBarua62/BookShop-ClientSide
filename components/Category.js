import Link from 'next/link';
import React from 'react';

const Category = () => {

    const categoryList = ["Women's Wear" , "Men's Style" , "Kid's Corner"];

    return (
        <div>
            {
                // categoryList.map(category => <Link href={`./category`}></Link>)
            }
        </div>
    );
};

export default Category;